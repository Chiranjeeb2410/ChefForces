#include<stdio.h>

int main()  {
    int count=0;
    int arr[10];
    for(int i=0; i<arr[i]; i++)    {
        if(i%2==0)  {
            count++;
        }
    }
    return count;
    printf("%d", count);
}
